 $('#my-items').append('<%= escape_javascript render(partial: "shared/item", collection: @items) %>');
 $('.pagination').replaceWith('<%= escape_javascript will_paginate(@items) %>');

<%=  "$(window).off('scroll');" unless @items.next_page %>

